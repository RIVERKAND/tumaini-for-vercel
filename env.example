JWT_SECRET=replace-this-with-a-long-random-string

# Storage — create a free Redis database at upstash.com (or via Vercel's
# Marketplace → Redis integration, which sets these for you automatically)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
